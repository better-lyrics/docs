---
import DocsLayout from '../layouts/DocsLayout.astro';
import Playground from '../components/Playground';
---

<DocsLayout title="Playground" wide pageActions={false}>
  <p>Enter the song, artist, album and duration to fetch synchronized lyrics.</p>
  <Playground client:only="react" />
</DocsLayout>
